# Copy this file to `bin/.env` (or export the values in your shell) so the
# OBS MCP server can authenticate against your OBS Studio instance.
#
# The MCP server reads these in `ObsMcpServer.bootstrapFromEnv()` and will
# automatically connect at startup when OBS_WEBSOCKET_URL is set.

# Full URL of the OBS WebSocket server. `ws://host:port` or `wss://host:port`.
OBS_WEBSOCKET_URL=ws://localhost:4455

# Password configured in OBS Studio → Tools → WebSocket Server Settings.
# Leave unset/blank for anonymous (auth-disabled) servers.
OBS_WEBSOCKET_PASSWORD=

# Connect timeout in seconds. Optional; defaults to 120.
OBS_WEBSOCKET_TIMEOUT=120
